<%= page_title(title: "Inbound SIP Trunks") do %>
  <%= render("shared/filters") do |form| %>
    <%= form.date_range title: "Created At", filter_value: [params.dig(:filter, :from_date), params.dig(:filter, :to_date)] %>
  <% end %>
  <%= render("shared/export_action", export_params: { resource_type: "InboundSIPTrunk" }) %>
  <% if policy(:inbound_sip_trunk).new? %>
    <%= render("shared/new_resource_page_action", path: new_dashboard_inbound_sip_trunk_path) %>
  <% end %>
<% end %>

<div class="card-body">
  <%= render "collection", resources: @resources %>

  <%= render "shared/pagination", resources: @resources %>
</div>
