<%= turbo_stream.append "tiers" do %>
  <%= simple_fields_for(:tariff_plan) do |parent_form| %>
    <%= parent_form.simple_fields_for(:tiers, @resource, index: (Time.current.to_f * 1000).to_i, wrapper: :input_group) do |form| %>
      <%= render "fields", form: %>
    <% end %>
  <% end %>
<% end %>
