<%= page_title(title: "Edit Account") do %>
  <% if policy(@resource.account).destroy? %>
    <%= render("shared/destroy_resource_page_action", path: dashboard_account_path(@resource)) %>
  <% end %>
<% end %>

<%= render "form", resource: @resource %>
