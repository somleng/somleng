<%= page_title(title: "Edit Account Settings") %>

<div class="card-body">
  <%= simple_form_for(@resource, url: dashboard_account_settings_path, wrapper: :input_group) do |f| %>
    <div class="row">
      <div class="col-sm-6">
        <%= f.input :name, maxlength: 128, hint: "A friendly name for your account" %>
      </div>
    </div>

    <%= f.button :submit %>
  <% end %>
</div>
