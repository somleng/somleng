<p>Hello <%= @resource.name %>!</p>

<p>Your account has been locked due to an excessive number of unsuccessful sign in attempts.</p>

<%= link_to "Unlock my account", unlock_url(@resource, unlock_token: @token, host: @host), class: "btn btn-primary p-3 fw-700 ax-center" %>
