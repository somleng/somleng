{
  "name": "Twilreapi",
  "description": "An Open Source implementation of Twillo's REST API",
  "website": "https://twilreapi.herokuapp.com",
  "repository": "https://github.com/dwilkie/twilreapi",
  "scripts": {
    "postdeploy": "bundle exec rake db:schema:load && bundle exec rake db:seed"
  },
  "env": {
    "SECRET_KEY_BASE": {
      "description": "A secret key for verifying the integrity of signed cookies.",
      "generator": "secret"
    },
    "WEB_CONCURRENCY": {
      "description": "The number of processes to run.",
      "value": "5"
    },
    "ACTIVE_JOB_QUEUE_ADAPTER": {
      "description": "The queue adapter to use with ActiveJob",
      "value": "sidekiq"
    },
    "ACTIVE_JOB_OUTBOUND_CALL_QUEUE": {
      "description": "The name of the queue to enqueue outbound calls",
      "value": "outbound_call_queue"
    },
    "SIDEKIQ_WEB_HTTP_BASIC_USERNAME": {
      "description": "The HTTP Basic username for authentication for Sidekiq's Web Interface (only applicable if your using Sidekiq)",
      "value": "sidekiq_user_change_me"
    },
    "SIDEKIQ_WEB_HTTP_BASIC_PASSWORD": {
      "description": "The HTTP Basic password for authentication for Sidekiq's Web Interface (only applicable if your using Sidekiq)",
      "value": "sidekiq_password_change_me"
    }
  },
  "addons": [
    "heroku-redis:hobby-dev"
  ]
}
